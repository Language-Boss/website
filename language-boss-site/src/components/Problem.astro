---
const requirements = [
  'Make teaching smoother, not more complicated',
  'Turn homework into insight, not busywork',
  'Keep quality consistent across classes and campuses',
  'Prove progress to parents and stakeholders',
];
---

<section class="problem" id="problem">
  <div class="container">
    <div class="problem-grid">
      <div class="problem-content">
        <p class="eyebrow">The Problem</p>
        <h2 class="problem-title">
          Schools don't need another app—they need a system that <em>works</em>.
        </h2>
      </div>
      
      <div class="problem-cards">
        <div class="problem-card">
          <h3 class="card-title">The system must:</h3>
          <ul class="requirements-list">
            {requirements.map((req, i) => (
              <li style={`--index: ${i}`}>
                <span class="req-icon">
                  <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                    <path d="M13.5 4.5L6 12L2.5 8.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </span>
                <span>{req}</span>
              </li>
            ))}
          </ul>
        </div>
        
        <div class="promise-card">
          <p class="eyebrow">The Promise</p>
          <h3 class="promise-title">
            Teach better. Operate smarter. Grow with confidence.
          </h3>
          <p class="promise-body">
            Language Boss helps schools increase teaching effectiveness, strengthen retention, 
            and improve unit economics—without burning out teachers.
          </p>
        </div>
      </div>
    </div>
  </div>
  
  <div class="problem-decoration">
    <div class="deco-line"></div>
  </div>
</section>

<style>
  .problem {
    position: relative;
    padding: 6rem 0;
    background: var(--color-paper);
    overflow: hidden;
  }
  
  .problem-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 4rem;
    align-items: start;
  }
  
  .problem-content {
    position: sticky;
    top: 8rem;
  }
  
  .problem-title {
    font-size: clamp(2rem, 3.5vw, 2.75rem);
    line-height: 1.2;
    color: var(--color-ink);
  }
  
  .problem-title em {
    font-style: normal;
    color: var(--color-accent);
    position: relative;
  }
  
  .problem-title em::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 8px;
    background: var(--color-accent-glow);
    border-radius: 4px;
    z-index: -1;
  }
  
  .problem-cards {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
  
  .problem-card {
    background: white;
    border-radius: var(--radius-lg);
    padding: 2rem;
    box-shadow: 
      0 2px 8px rgba(10, 22, 40, 0.04),
      0 12px 40px rgba(10, 22, 40, 0.06);
  }
  
  .card-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-ink);
    margin-bottom: 1.5rem;
  }
  
  .requirements-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  
  .requirements-list li {
    display: flex;
    align-items: flex-start;
    gap: 0.875rem;
    font-size: 1rem;
    color: var(--color-ink-light);
  }
  
  .req-icon {
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-mint);
    color: white;
    border-radius: 50%;
    margin-top: 2px;
  }
  
  .promise-card {
    background: var(--color-ink);
    border-radius: var(--radius-lg);
    padding: 2.5rem;
    color: var(--color-paper);
  }
  
  .promise-card .eyebrow {
    color: var(--color-mint);
  }
  
  .promise-title {
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 1rem;
    color: white;
  }
  
  .promise-body {
    font-size: 1rem;
    line-height: 1.7;
    color: rgba(250, 249, 247, 0.8);
  }
  
  .problem-decoration {
    position: absolute;
    top: 50%;
    right: 0;
    transform: translateY(-50%);
    width: 200px;
    height: 400px;
    overflow: hidden;
    pointer-events: none;
  }
  
  .deco-line {
    position: absolute;
    top: 0;
    right: -50px;
    width: 400px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--color-accent-glow), transparent);
    transform: rotate(45deg);
    transform-origin: right center;
  }
  
  @media (max-width: 900px) {
    .problem-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    
    .problem-content {
      position: static;
      text-align: center;
    }
    
    .problem-title em::after {
      display: none;
    }
  }
</style>

