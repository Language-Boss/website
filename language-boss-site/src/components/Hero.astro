---
const stats = [
  { label: 'Grammar accuracy lift', value: '+12%', sub: 'in two weeks' },
  { label: 'Speaking confidence', value: '2.3x', sub: 'after AI drills' },
  { label: 'Grading time saved', value: '-65%', sub: 'auto-scoring' },
];

const steps = [
  {
    title: 'Import textbooks or PDFs',
    desc: 'AI reads vocab, grammar, and themes to build a class knowledge graph.'
  },
  {
    title: 'Generate practice & speech scripts',
    desc: 'Dialogue, fill-ins, corrections, synonyms, short writing—all auto-generated.'
  },
  {
    title: 'Teachers refine and launch',
    desc: 'Core homework plus personal boosters, with US/UK accent detection.'
  },
];
---

<section class="hero" id="top">
  <div class="hero-bg">
    <div class="hero-gradient"></div>
    <div class="hero-grid-pattern"></div>
    <div class="hero-orb hero-orb-1"></div>
    <div class="hero-orb hero-orb-2"></div>
  </div>
  
  <div class="hero-container">
    <div class="hero-content">
      <p class="eyebrow animate-fade-in-up">Run your school on one system</p>
      <h1 class="hero-title animate-fade-in-up animate-delay-1">
        Not another app.<br />
        <span class="hero-title-accent">A complete operating system</span> for your English program.
      </h1>
      <div class="hero-lede animate-fade-in-up animate-delay-2">
        <p>Parents don't want promises. They want <strong>progress they can see</strong>.</p>
        <p class="hero-lede-sub">Stronger engagement + clear progress = higher retention and revenue.</p>
      </div>
      <div class="hero-actions animate-fade-in-up animate-delay-3">
        <a href="#demo" class="btn btn-primary">Book a Demo</a>
        <a href="#why" class="btn btn-secondary">Learn More</a>
      </div>
      <div class="hero-badge animate-fade-in-up animate-delay-4">
        <div class="hero-badge-icon">LB</div>
        <p class="hero-badge-text">
          Built by native English educators who understand Taiwan—bilingual, local-first, outcomes-first.
        </p>
      </div>
    </div>
    
    <div class="hero-card animate-fade-in-up animate-delay-3">
      <div class="card-shine"></div>
      <div class="card-header">
        <div class="card-status">
          <span class="status-dot"></span>
          <span>Progress you can prove</span>
        </div>
      </div>
      
      <div class="stats-grid">
        {stats.map((stat, i) => (
          <div class="stat-item" style={`--delay: ${i * 0.1}s`}>
            <span class="stat-label">{stat.label}</span>
            <span class="stat-value">{stat.value}</span>
            <span class="stat-sub">{stat.sub}</span>
          </div>
        ))}
      </div>
      
      <div class="timeline">
        {steps.map((step, i) => (
          <div class="timeline-step" style={`--delay: ${0.3 + i * 0.15}s`}>
            <div class="timeline-marker">
              <span class="timeline-number">{i + 1}</span>
            </div>
            <div class="timeline-content">
              <h4 class="timeline-title">{step.title}</h4>
              <p class="timeline-desc">{step.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 8rem 0 5rem;
    overflow: hidden;
  }
  
  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
  }
  
  .hero-gradient {
    position: absolute;
    inset: 0;
    background: 
      radial-gradient(ellipse 80% 60% at 70% 20%, rgba(78, 205, 196, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse 60% 50% at 20% 80%, rgba(232, 93, 76, 0.06) 0%, transparent 50%),
      linear-gradient(180deg, var(--color-paper) 0%, var(--color-paper-warm) 100%);
  }
  
  .hero-grid-pattern {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(10, 22, 40, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(10, 22, 40, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 60% at 50% 40%, black, transparent);
  }
  
  .hero-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    animation: float 8s ease-in-out infinite;
  }
  
  .hero-orb-1 {
    width: 500px;
    height: 500px;
    top: -10%;
    right: -10%;
    background: radial-gradient(circle, rgba(78, 205, 196, 0.15) 0%, transparent 70%);
    animation-delay: 0s;
  }
  
  .hero-orb-2 {
    width: 400px;
    height: 400px;
    bottom: -5%;
    left: -5%;
    background: radial-gradient(circle, rgba(232, 93, 76, 0.1) 0%, transparent 70%);
    animation-delay: -4s;
  }
  
  .hero-container {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  
  .hero-content {
    max-width: 560px;
  }
  
  .hero-title {
    margin-bottom: 1.5rem;
    color: var(--color-ink);
  }
  
  .hero-title-accent {
    color: var(--color-accent);
  }
  
  .hero-lede {
    font-size: 1.125rem;
    color: var(--color-ink-light);
    margin-bottom: 2rem;
    line-height: 1.7;
  }
  
  .hero-lede strong {
    color: var(--color-ink);
    font-weight: 600;
  }
  
  .hero-lede-sub {
    margin-top: 0.75rem;
    font-size: 1rem;
    color: var(--color-ink-subtle);
  }
  
  .hero-actions {
    display: flex;
    gap: 1rem;
    margin-bottom: 2.5rem;
    flex-wrap: wrap;
  }
  
  .hero-badge {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1rem 1.25rem;
    background: rgba(255, 255, 255, 0.6);
    backdrop-filter: blur(10px);
    border-radius: var(--radius-md);
    border: 1px solid rgba(10, 22, 40, 0.06);
  }
  
  .hero-badge-icon {
    flex-shrink: 0;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-ink);
    color: var(--color-paper);
    font-weight: 700;
    font-size: 0.75rem;
    border-radius: 8px;
  }
  
  .hero-badge-text {
    font-size: 0.875rem;
    color: var(--color-ink-light);
    line-height: 1.5;
  }
  
  /* Stats Card */
  .hero-card {
    position: relative;
    background: white;
    border-radius: var(--radius-lg);
    padding: 2rem;
    box-shadow: 
      0 4px 12px rgba(10, 22, 40, 0.04),
      0 20px 50px rgba(10, 22, 40, 0.08);
    overflow: hidden;
  }
  
  .card-shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 255, 255, 0.4) 50%,
      transparent 100%
    );
    animation: shine 3s ease-in-out infinite;
    animation-delay: 1s;
  }
  
  @keyframes shine {
    0% { left: -100%; }
    20% { left: 100%; }
    100% { left: 100%; }
  }
  
  .card-header {
    margin-bottom: 1.5rem;
  }
  
  .card-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-ink-light);
  }
  
  .status-dot {
    width: 8px;
    height: 8px;
    background: var(--color-mint);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }
  
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-bottom: 2rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(10, 22, 40, 0.08);
  }
  
  .stat-item {
    text-align: center;
  }
  
  .stat-label {
    display: block;
    font-size: 0.75rem;
    color: var(--color-ink-subtle);
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .stat-value {
    display: block;
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-accent);
    line-height: 1;
    margin-bottom: 0.25rem;
  }
  
  .stat-sub {
    display: block;
    font-size: 0.75rem;
    color: var(--color-ink-subtle);
  }
  
  .timeline {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
  
  .timeline-step {
    display: flex;
    gap: 1rem;
  }
  
  .timeline-marker {
    flex-shrink: 0;
    position: relative;
  }
  
  .timeline-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    background: var(--color-ink);
    color: white;
    font-size: 0.8125rem;
    font-weight: 600;
    border-radius: 50%;
  }
  
  .timeline-step:not(:last-child) .timeline-marker::after {
    content: '';
    position: absolute;
    top: 32px;
    left: 50%;
    transform: translateX(-50%);
    width: 2px;
    height: calc(100% + 1.25rem - 4px);
    background: linear-gradient(to bottom, var(--color-ink-subtle), transparent);
  }
  
  .timeline-content {
    padding-top: 2px;
  }
  
  .timeline-title {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-ink);
    margin-bottom: 0.25rem;
  }
  
  .timeline-desc {
    font-size: 0.8125rem;
    color: var(--color-ink-subtle);
    line-height: 1.5;
  }
  
  @media (max-width: 1024px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    
    .hero-content {
      max-width: 100%;
      text-align: center;
    }
    
    .hero-actions {
      justify-content: center;
    }
    
    .hero-badge {
      justify-content: center;
      text-align: left;
    }
  }
  
  @media (max-width: 640px) {
    .hero {
      padding: 7rem 0 3rem;
    }
    
    .stats-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
    
    .stat-value {
      font-size: 2.5rem;
    }
    
    .hero-badge {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
  }
</style>

